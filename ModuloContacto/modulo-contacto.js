function initModuloContacto() {
    console.log("Modulo Contacto Inicializado");
}

// No auto-inicializar, script.js lo hará cuando el HTML esté cargado
// if (document.readyState === 'loading') { ... }
