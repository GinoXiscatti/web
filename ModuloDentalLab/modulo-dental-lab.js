function initModuloDentalLab() {
    console.log("Modulo Dental Lab Inicializado");
}

// No auto-inicializar, script.js lo hará cuando el HTML esté cargado
// if (document.readyState === 'loading') { ... }
